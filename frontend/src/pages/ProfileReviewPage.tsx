export default function ProfileReviewPage() {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold tracking-tight">Profile review</h1>
      <p className="max-w-prose text-slate-600">
        The extracted profile with each field labelled by status (extracted / inferred /
        missing) lands here. (Slice 1)
      </p>
    </section>
  )
}
